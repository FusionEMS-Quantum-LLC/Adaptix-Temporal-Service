# Adaptix Temporal Service — Python dependencies
#
# Pin minor versions for reproducible builds.
# Keep in sync with pyproject.toml [project.dependencies].
# boto3 and botocore are included for AWS Secrets Manager access
# if future activity implementations need direct AWS SDK calls.
#
# cryptography provides the AES-256-GCM primitive behind temporal_app/codec.py.
# It is a RUNTIME dependency, not a dev tool: without it the worker cannot
# encrypt Temporal payloads and refuses to start. The Dockerfile installs from
# this file, so it must be listed here and not only in pyproject.toml.
temporalio>=1.31.0,<2.0.0
httpx>=0.28.1,<1.0.0
boto3>=1.43.67,<2.0.0
botocore>=1.43.72,<2.0.0
cryptography>=50.0.0,<51.0.0
